name: build and push all

on:
  workflow_call:

jobs:
  build-and-push:
    name: build and push ${{ matrix.microservice }}
    uses: ./.github/workflows/build-and-push.yml
    strategy:
      matrix:
        microservice: ["react", "angular", "dotnet", "grafana", "prometheus"]
    with:
      microservice: ${{ matrix.microservice }}
    secrets: inherit